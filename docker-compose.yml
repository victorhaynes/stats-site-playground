version: '3'

services:
  backend:
    build:
      context: ./backend
    command: gunicorn wrs_api.wsgi --bind 0.0.0.0:8000
    ports:
    - "8000:8000"
  frontend:
    build:
      context: ./client/winratesgg
    volumes:
      - react_build:/client/build
  nginx:
    image: nginx:latest
    ports:
      - 80:8080
    volumes:
      - ./nginx/nginx-setup.conf:/etc/nginx/conf.d/default.conf:ro
      - react_build:/var/www/client
    depends_on:
    - backend
    - frontend
  cronjob:
    build:
      context: ./content_scheduler
    volumes:
    - cron_logs_icons:/var/log/icons


volumes:
  react_build:
  cron_logs_icons:

